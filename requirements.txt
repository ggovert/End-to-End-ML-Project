from setuptools import find_packages, setup

setup(
    name = "mlproject",
    version="0.0.1",
    author="Gio",
    author_email="gio@gmail.com",
    packages = find_packages(),
    install_requirements= ["pandas", "numpy", "seaborn"]
)